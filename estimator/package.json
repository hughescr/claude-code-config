{
  "name": "estimator",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Token-based task estimation and tracking for Claude Code (Phase 0: collector).",
  "engines": {
    "bun": ">=1.2.0"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/bun": "^1.3.14",
    "typescript": "^7.0.2"
  },
  "scripts": {
    "init": "bun run src/cli.ts init",
    "sweep": "bun run src/cli.ts sweep",
    "backfill": "bun run src/cli.ts backfill",
    "prices": "bun run src/cli.ts prices",
    "census": "bun run src/cli.ts census",
    "check:schema": "bun run scripts/check-schema.ts",
    "typecheck": "tsc --noEmit",
    "test": "bun test"
  }
}
